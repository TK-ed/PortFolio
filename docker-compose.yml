services:
  app:
    build: .
    env_file:
      - ./.env.local.example
    container_name: app
    volumes:
      - .:/usr/src
      - /usr/src/node_modules
      - /usr/src/.next
    ports:
      - 3000:3000
    command: npm run dev
    restart: always
